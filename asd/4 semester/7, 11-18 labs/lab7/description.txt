Условие: Реализовать алгоритм Прима нахождения минимального покрывающего дерева.

Алгоритм Прима работает по следующему принципу:
1. Выбирается произвольная стартовая вершина.
2. Создается список доступных ребер, которые не входят в дерево, и сортируется по возрастанию весов.
3. Из списка доступных ребер выбирается ребро с наименьшим весом и добавляется в дерево.
4. Добавляются все вершины, соединенные выбранным ребром, в список посещенных.
5. Обновляется список доступных ребер, исключая те, которые соединены с вершинами, уже находящимися в дереве.
6. Выполняются шаги 3-5, пока не будут рассмотрены все вершины графа.

edges инициализируют переменные для реализации алгоритма Прима. edges представляет собой список рёбер
минимального остовного дерева, visited - список посещенных вершин, а available_edges - список доступных рёбер
для добавления в дерево.
цикл выполняет алгоритм Прима для построения минимального остовного дерева. На каждой итерации выбирается ребро
с наименьшим весом из доступных рёбер, добавляется в edges, и соответствующая вершина добавляется в visited.